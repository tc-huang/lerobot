🔄 **{{ lang_tag }} translations re-synced** — deployment <{{ preview_url }}/>

{% for row in rows %}{{ row.emoji }} {{ row.count }} {{ row.comment_label }}{% if not loop.last %} · {% endif %}{% endfor %}


To review: {{ to_review }}.

{% if last_sync %}
English sources moved from {{ last_sync_commit }} to {{ source }}.
{% endif %}
{% if needs_attention %}

{% for doc in needs_attention %}{{ doc.emoji }} `{{ doc.name }}`{% if not loop.last %}, {% endif %}{% endfor %} keep their previous translation.
{% endif %}

The branch was force-pushed and the PR description now describes this run.
