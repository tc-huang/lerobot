# HopeJR[[hopejr]]

## 先决条件[[prerequisites]]

- [硬件设置](https://github.com/TheRobotStudio/HOPEJr)

## 安装 LeRobot[[install-lerobot]]

按照[安装说明](https://github.com/huggingface/lerobot#installation)安装 LeRobot。

使用 HopeJR 依赖项安装 LeRobot：

```bash
pip install -e ".[hopejr]"
```

## 设备配置[[device-configuration]]

在开始校准和操作之前，你需要确定每个 HopeJR 组件的 USB 端口。运行此脚本以查找机械臂、手部、手套和外骨骼（exoskeleton）的 USB 端口：

```bash
lerobot-find-port
```

这将显示可用的 USB 端口及其关联设备。记下端口路径（例如 `/dev/tty.usbmodem58760433331`、`/dev/tty.usbmodem11301`），因为在记录数据、回放片段（episodes）或运行遥操作（teleoperation）脚本时，你需要在 `--robot.port` 和 `--teleop.port` 参数中指定这些路径。

## 步骤 1：校准[[step-1-calibration]]

在执行遥操作之前，需要校准 HopeJR 的肢体。校准文件将保存到 `~/.cache/huggingface/lerobot/calibration`

### 1.1 校准机器人手[[11-calibrate-robot-hand]]

```bash
lerobot-calibrate \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=blue \
    --robot.side=right
```

运行校准脚本时，将弹出校准 GUI。手指关节的命名如下：

**拇指**：

- **CMC**：连接拇指与手部的基部关节
- **MCP**：指节关节
- **PIP**：第一指关节
- **DIP**：指尖关节

**食指、中指、无名指和小指**：

- **桡侧屈肌**：使手指基部向拇指移动
- **尺侧屈肌**：使手指基部向小指移动
- **PIP/DIP**：弯曲手指的远端指骨和近端指骨

每个关节都需要通过 GUI 单独校准。
请注意，尺侧屈肌和桡侧屈肌应具有相同大小的范围（但偏移不同），以获得对称运动。

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_1.png"
    alt="在手部校准 GUI 中设置边界"
    title="在手部校准 GUI 中设置边界"
    width="100%"
  ></img>
</p>

使用校准界面为每个关节设置范围边界，如上所示。

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_2.png"
    alt="保存校准值"
    title="保存校准值"
    width="100%"
  ></img>
</p>

为所有关节设置合适的边界后，点击“保存”将校准值保存到电机。

### 1.2 校准遥操作手套[[12-calibrate-teleoperator-glove]]

```bash
lerobot-calibrate \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=red \
    --teleop.side=right
```

从拇指开始，让每根手指完成其整个运动范围。

```
Move thumb through its entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
NAME      |    MIN |    POS |    MAX
thumb_cmc |   1790 |   1831 |   1853
thumb_mcp |   1497 |   1514 |   1528
thumb_pip |   1466 |   1496 |   1515
thumb_dip |   1463 |   1484 |   1514
```

继续处理每根手指：

```
Move middle through its entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
NAME                 |    MIN |    POS |    MAX
middle_mcp_abduction |   1598 |   1718 |   1820
middle_mcp_flexion   |   1512 |   1658 |   2136
middle_dip           |   1484 |   1500 |   1547
```

校准完成后，系统会将校准保存到 `/Users/your_username/.cache/huggingface/lerobot/calibration/teleoperators/homunculus_glove/red.json`

### 1.3 校准机器人手臂[[13-calibrate-robot-arm]]

```bash
lerobot-calibrate \
    --robot.type=hope_jr_arm \
    --robot.port=/dev/tty.usbserial-1110 \
    --robot.id=white
```

这将打开一个校准 GUI，你可以在其中为每个电机设置范围限制。手臂运动按如下方式组织：

- **肩部**：俯仰、偏航和横滚
- **肘部**：弯曲
- **腕部**：俯仰、偏航和横滚

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_2.png"
    alt="在手臂校准 GUI 中设置边界"
    title="在手臂校准 GUI 中设置边界"
    width="100%"
  ></img>
</p>

使用校准界面为每个关节设置范围边界。让每个关节完成其整个运动范围，并相应地调整最小值和最大值。为所有关节设置合适的边界后，保存校准。

### 1.4 校准遥操作外骨骼[[14-calibrate-teleoperator-exoskeleton]]

```bash
lerobot-calibrate \
    --teleop.type=homunculus_arm \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=black
```

外骨骼允许控制机器人手臂。校准期间，系统会提示你让所有关节完成其整个运动范围：

```
Move all joints through their entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
-------------------------------------------
NAME            |    MIN |    POS |    MAX
shoulder_pitch  |    586 |    736 |    895
shoulder_yaw    |   1257 |   1374 |   1390
shoulder_roll   |    449 |   1034 |   2564
elbow_flex      |   3023 |   3117 |   3134
wrist_roll      |   3073 |   3096 |   3147
wrist_yaw       |   2143 |   2171 |   2185
wrist_pitch     |   1975 |   1993 |   2074
Calibration saved to /Users/your_username/.cache/huggingface/lerobot/calibration/teleoperators/homunculus_arm/black.json
```

## 步骤 2：遥操作[[step-2-teleoperation]]

由于 Feetech 中间件中的全局变量冲突，手臂和手部的遥操作必须在单独的 shell 会话中运行：

### 手部[[hand]]

```bash
lerobot-teleoperate \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=blue \
    --robot.side=right \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=red \
    --teleop.side=right \
    --display_data=true \
    --fps=30
```

### 手臂[[arm]]

```bash
lerobot-teleoperate \
    --robot.type=hope_jr_arm \
    --robot.port=/dev/tty.usbserial-1110 \
    --robot.id=white \
    --teleop.type=homunculus_arm \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=black \
    --display_data=true \
    --fps=30
```

## 步骤 3：记录、回放、训练[[step-3-record-replay-train]]

使用 Hope-JR 进行记录、回放和训练仍处于实验阶段。

### 记录[[record]]

此步骤记录数据集（dataset），可[在此处](https://huggingface.co/datasets/nepyope/hand_record_test_with_video_data)查看示例。

```bash
lerobot-record \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=right \
    --robot.side=right \
    --robot.cameras='{"main": {"type": "opencv", "index_or_path": 0, "width": 640, "height": 480, "fps": 30}}' \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem1201 \
    --teleop.id=right \
    --teleop.side=right \
    --dataset.repo_id=<USER>/hand_record_test_with_video_data \
    --dataset.single_task="Hand recording test with video data" \
    --dataset.num_episodes=1 \
    --dataset.episode_time_s=5 \
    --dataset.push_to_hub=true \
    --dataset.private=true \
    --dataset.streaming_encoding=true \
    --dataset.encoder_threads=2 \
    --display_data=true
# 可选：--dataset.rgb_encoder.vcodec=auto
```

### 回放[[replay]]

```bash
lerobot-replay \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=right \
    --robot.side=right \
    --dataset.repo_id=<USER>/hand_record_test_with_camera \
    --dataset.episode=0
```

### 训练[[train]]

```bash
lerobot-train \
  --dataset.repo_id=<USER>/hand_record_test_with_video_data \
  --policy.type=act \
  --output_dir=outputs/train/hopejr_hand \
  --job_name=hopejr \
  --policy.device=mps \
  --wandb.enable=true \
  --policy.repo_id=<USER>/hand_test_policy
```

### 评估[[evaluate]]

此训练运行可[在此处](https://wandb.ai/tino/lerobot/runs/rp0k8zvw?nw=nwusertino)查看示例。

```bash
lerobot-record \
  --robot.type=hope_jr_hand \
  --robot.port=/dev/tty.usbmodem58760432281 \
  --robot.id=right \
  --robot.side=right \
  --robot.cameras='{"main": {"type": "opencv", "index_or_path": 0, "width": 640, "height": 480, "fps": 30}}' \
  --display_data=false \
  --dataset.repo_id=<USER>/eval_hopejr \
  --dataset.single_task="Evaluate hopejr hand policy" \
  --dataset.num_episodes=10 \
  --dataset.streaming_encoding=true \
  --dataset.encoder_threads=2 \
  --policy.path=outputs/train/hopejr_hand/checkpoints/last/pretrained_model
# 可选：--dataset.rgb_encoder.vcodec=auto
```
